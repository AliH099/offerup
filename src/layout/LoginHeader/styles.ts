import { Stack, styled } from '@mui/material';
import { grey } from '@mui/material/colors';

const LoginHeaderContainer = styled(Stack)(() => ({
    width: '100%',
    alignItems: 'center',
    padding: '10px',
    backgroundColor: grey[100],
}));
export default LoginHeaderContainer;
